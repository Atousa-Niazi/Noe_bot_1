# Noe_bot_1

you will need:
                !pip install pyTelegramBotAPI
                !pip install persiantools
                !pip install qrcode[pil]
                !pip install opencv-python
                !pip install pillow
                !pip install gTTS
